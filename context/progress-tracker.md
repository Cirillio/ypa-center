# Трекер состояния

Живой файл. Обновляется по ходу работы, а не в конце. Если он противоречит
коду – прав код, а файл чинится.

**Обновлён:** 2026-09-24 (конец дня)

---

## Текущая фаза

Скоуп разворачивался дважды за два дня – см. «Принятые решения». Итог на
сегодня: делаем **не только витрину**. В работе – флоу регистрации, редизайн
трёх страниц покупки (пробное, абонемент, событие) и часть личного кабинета
(столбец «Наши записи», удаление ребёнка, пагинация). Разделение
труда: бэк – друг (ветка `def`), фронт – здесь.

## Текущая цель

Реализовать план из 5 пунктов (ниже, «Следующие шаги»), синхронно с бэком.
Приёмка бэковой части – по `backend-checklist.md` (в корне `ypa-center.ru/`,
вне git, не отдавать другу заранее).

---

## Состояние репозиториев

| Репозиторий          | Ветка     | Состояние                                                                                                                                                                                                                                                            |
| -------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `frontend-core`      | `preprod` | впереди `origin/preprod` на 9 коммитов (не запушены), сессия 2026-09-24 закоммичена по темам. В рабочей директории две чужие правки, не вошедшие в коммиты: `app.config.ts` (стиль выпадающего списка) и `clubs/Card.vue` (`text-balance`). Push – только по просьбе |
| `ypa-center-backend` | `def`     | Переключились 2026-09-22 с `feat/frontend-integration`. Контейнеры (`docker compose`) на момент переключения **не подтверждены пересобранными** – нужно `up -d --build` перед работой                                                                                |

---

## Сделано

- Витрина: `/`, `/clubs`, `/teachers`, `/gallery`, `/about` – на живом API
- Личный кабинет `/login`, `/me` – на живом API, базовый функционал
- Слой данных: сервисы-классы, single-flight refresh, `parseApiError`,
  генерация типов из OpenAPI
- Формы связи с Turnstile и антиспам-кулдауном
- Аудит 73 компонентов – `tasks/component-audit-2026-09.md`, 15 находок закрыто
- **2026-09-21:** создан `context/` – 8 документов плюс снимки доков бэка
- **2026-09-22:** бэк исследован на живом коде ветки `def` (не со слов) –
  подтверждён рабочий чекаут абонемента/пробного с `Idempotency-Key`, найден
  недостающий `POST /checkout/event`
- **2026-09-22:** согласован с бэкендером план из 5 пунктов, оформлен
  `backend-checklist.md`
- **2026-09-22:** `context/` актуализирован под новый скоуп – этот файл,
  `project-overview.md`, `architecture.md` (§5–6), `ui-context.md` (§10),
  `api.md`, `content.md`, снимок `backend/`
- **2026-09-23:** уборка `tasks/` – `handoff-2026-09-21.md` удалён (всё из
  него перенесено в остальной `context/`, ничего актуального не осталось);
  `docs/payment-flow.md` перенесён в `tasks/payment-flow.md`, помечен
  устаревшим в части архитектуры; `component-audit-2026-09.md` – вычеркнуты
  находки, поглощённые редизайном покупок, и дубли с этим файлом
- **2026-09-23:** исправлен баг утилиты `.h-with-header` (`--header-height-full` → `--ui-header-height`); синхронизированы палитры Nuxt UI в `app.config.ts` (`primary: "orange"`, `secondary: "green"`) с токенами `tokens.css`
- **2026-09-23:** реорганизована структура `app/components/`: ликвидированы плоский корень (11 компонентов) и папка `app/`; компоненты разложены по категориям `layout/`, `ui/`, `promo/`, `shared/` (каноничный автоимпорт Nuxt 4 с префиксами `<Layout* />`, `<Ui* />`, `<Promo* />`, `<Shared* />`); вопрос 9 закрыт
- **2026-09-24:** синхронизирован корневой `README.md` (актуальная структура `app/components/`, `ssr` вместо `prerender`, `stores/`, `services/`, слоистая архитектура Nuxt вместо терминов FSD); очищены устаревшие дубли компонентов (ровно 73 компонента в проекте); статус репозитория в трекере синхронизирован с `origin/preprod`
- **2026-09-24:** реализован Шаг 1 («Флоу авторизации/регистрации», Задача A из `tasks/registration-and-bookings.md`). Компоненты входа организованы в `components/login/otp/Widget.vue` и `components/login/profile/Widget.vue` (устранён рудиментарный `GuestGate.vue` из `me/`, копирайтинг адаптирован под единый быстрый вход/регистрацию без пароля за 1–2 мин, убраны function-props); созданы `app/constants/referral-sources.ts`, Zod-схема `app/schemas/profile.schema.ts`, тип `ProfileCompletionPayload`, методы `isProfileComplete` и `completeProfile` (`MOCK(profile-referral)`) в `MeService`; в стор `auth.ts` добавлен шаг `profile` и проверка заполненности профиля; в `pages/me.vue` подключён гейт редиректа на `/login` при неполном профиле. `bun run check` пройден.
- **2026-09-24:** реализован Шаг 3 («Столбец «Наши записи» в кабинете и реорганизация `me/`», Задача B из `tasks/registration-and-bookings.md`). Создан доменный тип `MeBooking` в `types/index.ts`; создан мок `services/mocks/me-bookings.mock.ts` с `BookingItemDraftDto` и 7 реалистичными записями; в `services/me.service.ts` добавлены маппер `toBooking` и метод `getBookings` (`MOCK(bookings)`); созданы композаблы `useMeBookings.ts` и универсальный `useShowMore.ts`; компоненты кабинета декомпозированы по 4 подпапкам `components/me/` (`parent/`, `subscriptions/`, `bookings/`, `upcoming/`) с виджетами и карточками, устаревшие плоские файлы удалены; в `pages/me.vue` собрана адаптивная сетка с двумя колонками (`xl:grid-cols-2`) для абонементов и записей под блоком родителя; создан отчёт в `context/done/session-2026-09-24-auth-and-bookings.md`. `bun run check` пройден.
- **2026-09-24:** ревью сессии в браузере, исправлено: переход на `/me` после анкеты (emit терялся при размонтировании виджета, экран оставался пустым); цены «Наших записей» занижались в 100 раз (`formatRub` на рублях → новый `formatRubles`, им же выводится сумма абонемента); валидация анкеты переведена на «только при сабмите» (`validate-on="[]"`, `form.clear(name)` при вводе, фокус на первое ошибочное поле); `me.vue` собран через `MeParentWidget`; иконка налезала на текст в поле почты (глобальный `px-4!` перебивает отступ под иконку – иконка убрана). Проверено в браузере: вход с заполненным профилем, новая почта → анкета → `/me`, гейт `/me` → `/login` при пустом профиле.

- **2026-09-24:** задача 3 бэклога – `redirectFrom` после входа. Утилита `resolveRedirect` (`app/utils/resolve-redirect.ts`, белый список `/me`, `/enroll/trial`, `/enroll/subscription`), `middleware/auth.ts` и гейт профиля в `me.vue` передают `redirectFrom`, `login.vue` использует его во всех трёх переходах. `bun run check` пройден. Проверено в браузере: `/me` без токенов → `/login?redirectFrom=/me`; вход по коду с `redirectFrom=/enroll/trial` → `/enroll/trial`; `https://evil.example`, `//evil.example`, `/admin`, пустой параметр → `/me`, `/enroll/subscription` → `/enroll/subscription`. **Не проверено в браузере:** путь через анкету новой почтой (использует ту же `redirectTarget`, но создаёт нового родителя на бэке).
- **2026-09-24:** задача 1 бэклога – a11y `/login`, `/me`. Вложенные `<main>` заменены на `<section aria-label>` (лейаут уже даёт `<main>`); заголовки вкладок «Вход» и «Личный кабинет» через `useSeoMeta`; бейдж типа активности получил `role="img"` (снято `aria-prohibited-attr`); полупрозрачный текст `text-default/50|60|70` заменён на `text-muted` в `me/*`, `login/*` и `layout/Footer.vue` (4.7–4.9:1 против 2.5–3.9:1). `html-has-lang` и `document-title` – ложные срабатывания: после загрузки `lang="ru"` и заголовок на месте, `@nuxt/a11y` стартует до применения `head` на `ssr: false` страницах. Проверено axe-core в браузере: на `/me` осталось 8 нарушений контраста, на `/login` (шаг email) 7, все на брендовых цветах, см. вопрос 16. Шаги `code` и `profile` через axe не гонял (код OTP ограничен 5 запросами в час), там изменена только замена `text-muted`.
- **2026-09-24:** задача 2 бэклога – синхронизация сессии между вкладками. Плагин `plugins/auth-sync.client.ts` (слушатель `storage`, реакция на смену статуса сессии), `isAccessTokenKey` в `utils/auth-tokens.ts`, ротация refresh в `composables/useApi.ts` под Web Locks (`ypa-token-refresh`) с повторным использованием пары, уже обновлённой другой вкладкой. `bun run check` пройден. Проверено в двух вкладках браузера: вход в A переводит B с `/login` на `/me`; при испорченном access и одновременной перезагрузке обеих вкладок ушёл один POST `/token/refresh/`, обе остались на `/me`; выход в A уводит B на `/login`. **Не проверено:** гонка «до правки» не воспроизводилась (риск следует из настроек бэка и теста `test_refresh_rotation_blacklists_old_token`), в проверке гонки сетевой монитор второй вкладки не записал запросов, так что строго совпавшее по времени 401 в обеих вкладках не подтверждено; поведение в браузерах без Web Locks.
- **2026-09-24:** задача 4 бэклога – удалены мёртвые `types/status.ts` (9 типов, ни один не использовался ни напрямую, ни через баррель) и `MOCK_STATUS_DATA` из `constants/mock.ts` (245 строк). `MOCK_CLUBS_WITH_SLOTS` и `MOCK_WEEKLY_SLOTS` оставлены: их используют конструкторы покупки. Нарушение «один баррель типов» снято. `bun run check` пройден.
- **2026-09-24:** задача 5 бэклога – `gallery/ModalControls.vue` переведён с function-props на `defineEmits` (`prev`, `next`, `close`); видимость кнопок листания вынесена из «наличия функции» в булевы пропсы `hasPrev`/`hasNext`; `gallery/Modal.vue` обновлён. Вопрос 8 закрыт, других function-props и `on`-префиксов в `app/` нет (проверено `rg`).
- **2026-09-24:** сводка каталога на `/clubs` перенесена в секцию «Все кружки» (`clubs/List.vue`): крупный заголовок, «Всего направлений: N», «Возраст: от X до Y лет» (минимум `age_min` и максимум `age_max` по всем группам, через `utils/format-age-range.ts`), кнопка «К расписанию» на `/clubs#schedule` (скрыта, если расписание не загрузилось). Удалены мёртвые `clubs/SectionCTA.vue`, пропсы `clubsLength`/`minAge` у `Hero` и computed'ы в `pages/clubs.vue`, закомментированный блок заголовка. Секции расписания добавлен `scroll-mt-(--ui-header-height)`, чтобы якорь не уходил под шапку. `bun run check` пройден; проверено в браузере на десктопе и 375px: значения верные, кнопка прокручивает к расписанию, переполнения нет.

## В работе

- Шаги 1 и 3 плана и задачи 1–5 бэклога выполнены, проверены в браузере и
  закоммичены (не запушены). Шаг 2 (страницы покупки) заморожен
  владельцем.
- Ждут решения владельца: вопрос 12 (строки с ценами под hero `/clubs`),
  16 (контраст брендовых цветов), 17 (минимальный возраст: «от 4» в FAQ или «от 6» в данных).
- Бэклог – `tasks/frontend-backlog-2026-09.md`; итог дня – `done/session-2026-09-24-backlog.md`.

## Следующие шаги

Порядок – итог обсуждения с бэкендером 2026-09-22, приоритет фронта.

1. ~~**Флоу авторизации/регистрации.**~~ (выполнено 2026-09-24)
2. **Редизайн трёх страниц покупки** – `/enroll/trial`, `/enroll/subscription`,
   `/enroll/event` (заморожен/отложен владельцем проекта, не брать без прямого указания).
3. ~~**Столбец «Наши записи» в кабинете и реорганизация `me/`**~~ (выполнено 2026-09-24, на `MOCK(bookings)`)
4. **Текст с ценами под кнопками hero на `/clubs`** – сводка уже перенесена в
   секцию «Все кружки» (2026-09-24), по строкам с ценами решения нет, см.
   `ui-context.md` §10.
5. **Флоу оплаты – после того, как страницы готовы.** Оплата события ждёт
   отдельно бэк (`POST /checkout/event` ещё не существует).

Мелкие задачи без блокеров (найдены 2026-09-24, детали – «Технический долг»):

- Возраст на карточках кружков (`clubs/Card.vue`, `age_min`–`age_max` группы) – после вопроса 17.
- `text-default/50|60|70` → `text-muted` на публичных страницах (13 мест, механическая замена).
- Двойное имя сайта в `title` вкладки (страницы кладут его сами + `titleTemplate`).

Параллельно у бэка (не наша работа, но блокирует пункты 2–5): удаление
ребёнка, пагинация, единый эндпоинт-агрегатор пробных+событий, доводка
регистрации – весь список в `backend-checklist.md`.

---

## Открытые вопросы

Закрывает владелец проекта, не агент. Дата – когда вопрос поставлен.

| #   | Вопрос                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Поставлен  | Блокирует                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------ |
| 2   | **Где какое название центра уместно:** «Улица Радости» (контакты, доки бэка) или полное «Центр умного развития «УРА»» (корневой `README.md`)? Смысл названия прояснён 2026-09-22 («У»лица «РА»дости), но не решено, где как показывать                                                                                                                                                                                                                                     | 2026-09-21 | Тексты, SEO, OG                |
| 3   | **Прод-хостинг, домен, процесс выкладки** – нигде не описаны                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-09-21 | Запуск                         |
| 7   | **Нужен ли `GET /public/settings/`** для управления hero-текстами без деплоя (`backend/admin.md`, последний раздел)                                                                                                                                                                                                                                                                                                                                                        | 2026-09-21 | –                              |
| 10  | **Контракт «профиль пуст → нужна анкета»** – каким полем/флагом `GET /me/profile/` это показывает? Уточнить у автора бэка                                                                                                                                                                                                                                                                                                                                                  | 2026-09-22 | Флоу регистрации (шаг 1 плана) |
| 11  | **Название и форма ответа эндпоинта-агрегатора** `TRIAL + EVENT` (рабочее – `/me/bookings/`) – финальное имя и точный список полей DTO                                                                                                                                                                                                                                                                                                                                     | 2026-09-22 | Столбец «Наши записи» (шаг 3)  |
| 12  | **Строки с ценами под кнопками hero `/clubs`** («От 450 ₽ за занятие…», «Разово, 1 200 ₽…»): вписаны в шаблон вручную, хотя `trialLesson` есть в `app.config.ts`. Убрать, оставить с ценой из конфига или перенести                                                                                                                                                                                                                                                        | 2026-09-22 | Шаг 4 плана                    |
| 14  | **Состав «Наших записей»:** только будущие или и прошедшие; неоплаченные (`PENDING_PAYMENT`) и отменённые; что показывать как участника у события                                                                                                                                                                                                                                                                                                                          | 2026-09-24 | Макет карточки записи (шаг 3)  |
| 16  | **Контраст брендовых цветов** (WCAG AA, 4.5:1): оранжевый `#f16d22` на кремовом и в soft-кнопках даёт 2.6–3.0, синий `info` `#2b7fff` даёт 3.2–3.5, белый на зелёном `#2d915d` даёт 3.94. Затронуты кнопки «Получить код», «Абонемент», «Мой кабинет», «Написать нам», «Показать ещё», «Добавить ребёнка», ссылки VK/Telegram, логотип. Варианты: затемнить `--ui-primary` / `--ui-secondary` / `info` (меняется бренд), либо принять как исключение для крупных элементов | 2026-09-24 | Полная a11y-приёмка            |
| 17  | **Минимальный возраст:** FAQ (`app.config.ts`) и `project-overview.md` говорят «от 4 лет», а сводка «Все кружки» считает по группам из API (сейчас от 6 до 13). Что верно: поправить FAQ или добавить группы для 4–5 лет                                                                                                                                                                                                                                                   | 2026-09-24 | Тексты, доверие к сводке       |

Закрыт 2026-09-23: **структура папки `components/`** (был вопрос 9) – принят и реализован
каноничный для Nuxt 4 Вариант А (`layout/`, `ui/`, `promo/`, `shared/` + доменные папки),
плоский корень и `app/` устранены.
Закрыт 2026-09-24: вопрос 6 (`context/` коммитим вместе с кодом).
Закрыт 2026-09-24: вопрос 4 – тесты пока не нужны (решение владельца), E2E-задача снята с бэклога.
Закрыт 2026-09-24: вопрос 8 – единый стандарт `defineEmits` без `on`-префикса, `gallery/ModalControls.vue` приведён.
Закрыт 2026-09-24: вопрос 15 – редактирование ребёнка не нужно (решение владельца).
Закрыт 2026-09-24: вопрос 13 – обязательный чекбокс согласия на обработку ПД
в анкете **остаётся** (решение владельца).

Закрыты 2026-09-22: судьба `/login`, `/me`, `/enroll/*` в скоупе (были
вопросом 1) – решено, входят в работу, см. «Текущая фаза». Контент от
заказчика (был вопрос 5) переклассифицирован – это не блокирующий вопрос, а
известный список задач на финальный проход, см. `content.md` §9.

---

## Технический долг

Известные дефекты. Тестовые заглушки контента (фото, отдельные цены/тексты)
сюда **не входят** – это ожидаемо, список и логика – в `content.md`, не техдолг.

### Баги

| Что                                                                                                                                                  | Где                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| `POST /public/events/{event_id}/register/` существует на бэке, но ни один сервис фронта его не вызывает – форма `/enroll/event` не отправляет заявку | `app/services/events.service.ts` |

### Временные моки (снять до запуска)

Добавляются по ходу реализации `tasks/registration-and-bookings.md`. Перед
запуском `rg "MOCK\(" app` должен быть пуст.

| Что                                                                                                                                                        | Где                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `MOCK(profile-referral)` – «откуда узнали» не уходит на бэк (поля нет в модели `Parent`)                                                                   | `app/services/me.service.ts`                                           |
| `MOCK(bookings)` – `/me/bookings/` не существует, данные из мока. Одинаковые 7 записей видит **любой** родитель, включая нового без детей («Иван», «Анна») | `app/services/me.service.ts`, `app/services/mocks/me-bookings.mock.ts` |

### Моки конструкторов вне конвенции

| Что                                                                                                                                                                                   | Где                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `MOCK_CLUBS_WITH_SLOTS`, `MOCK_WEEKLY_SLOTS` лежат в `constants/`, а не в `services/mocks/`, и без маркера `MOCK(`: `rg "MOCK\("` их не найдёт. Разобрать в шаге 2 (редизайн покупок) | `app/constants/mock.ts`, `useTrialEnrollment`, `useSubscriptionEnrollment` |

### Расхождения документации

| Что                                                                                                     | Где         |
| ------------------------------------------------------------------------------------------------------- | ----------- |
| Корневой `README.md` называет центр «Центр Умного Развития «УРА»» без пояснения – см. открытый вопрос 2 | `README.md` |

### Мелкое

| Что                                                                                                                                                                                                                                                          | Где                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------ |
| Полупрозрачный текст `text-default/50\|60\|70` (контраст ниже AA) остался вне кабинета и входа: `home/`, `event/`, `subscription/`, `callback/`, `feedback/`, `shared/` (13 мест, `rg "text-default/(50\|60\|70)" app`). Замена на `text-muted` механическая | `app/components/`                    |
| Двойное имя сайта в заголовке вкладки: страницы кладут «– Улица Радости» в `title` (`about.vue` и др.), а `titleTemplate` в `app.vue` добавляет его ещё раз («Пробное занятие – Улица Радости – Улица Радости»)                                              | `app/pages/*`, `app/app.vue`         |
| Стор `mobile-menu-store.ts` с суффиксом `-store`, второй стор – просто `auth.ts`                                                                                                                                                                             | `app/stores/`                        |
| `/checkout/subscription` – единственный путь без завершающего слеша                                                                                                                                                                                          | бэк                                  |
| `GET /public/activities/{id}/` существует, но не используется                                                                                                                                                                                                | `app/services/activities.service.ts` |

Открытые находки аудита компонентов (дублирование, a11y, хвосты) – в
`tasks/component-audit-2026-09.md` §3, сюда не дублируются.

---

## Принятые решения

| Дата       | Решение                                                                                                                                                             | Почему                                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-24 | Редактирование ребёнка в кабинете не делаем; ошибку исправляют удалением и повторным добавлением. `PATCH /me/children/{id}/` на фронте не используется              | Решение владельца: ребёнка нельзя трогать, пока он к чему-то записан, а пересоздать запись за секунду. Зависит от эндпоинта удаления на бэке |
| 2026-09-24 | Тесты (Vitest, Playwright) пока не нужны, E2E-сценарий регистрации не делаем                                                                                        | Решение владельца; проверка остаётся `bun run check` плюс браузер                                                                            |
| 2026-09-24 | Единый стандарт контрактов компонентов: `defineEmits`, имена событий без `on`-префикса, function-props не используем                                                | Уже зафиксировано в `code-standards.md` §3, вопрос 8 закрыт приведением последнего нарушителя                                                |
| 2026-09-24 | В анкете обязательный чекбокс согласия на обработку ПД со ссылками на `/consent` и `/privacy`                                                                       | Решение владельца: собираем ФИО и телефон; согласие на бэк пока не уходит (поля нет)                                                         |
| 2026-09-24 | Модульная структура `components/me/` (`parent/`, `subscriptions/`, `bookings/`, `upcoming/`) с паттерном `Widget.vue` и `Card.vue`                                  | Устраняет god-компоненты и плоский список из 10+ файлов, делает блоки изолированными и поддерживаемыми, сохраняет Nuxt 4 автоимпорт          |
| 2026-09-24 | Шаг 2 (редизайн покупок) временно заморожен, прямой переход к Шагу 3                                                                                                | Решение владельца проекта                                                                                                                    |
| 2026-09-24 | Моки до готовности бэка живут только в `app/services/mocks/`, помечены `MOCK(<id>)`, доменные типы постоянные, черновик DTO – внутри мока                           | Удаление мока и интеграция бэка правкой одного метода сервиса, без затрагивания UI; `rg "MOCK\("` – чек-лист удаления                        |
| 2026-09-24 | Анкета регистрации – после кода (email → код → анкета), почта в ней только для чтения; ФИО и телефон – реальный `PATCH /me/profile/`, мокается лишь «откуда узнали» | Соответствует решению бэка (`auth-flow.md` §4); полный мок оставил бы профиль пустым и зациклил гейт                                         |
| 2026-09-24 | `/me`: «Наши записи» – вторая колонка рядом с абонементами внутри левых `col-span-5`; лента «Ближайшие» остаётся справа                                             | Решение владельца                                                                                                                            |
| 2026-09-23 | Каноничная раскладка общих компонентов в Nuxt 4: `layout/`, `ui/`, `promo/`, `shared/` вместо плоского корня и `app/`                                               | Устраняет неоднозначность префиксов, ликвидирует мусорную папку `app/` без кастомных хаков автоимпорта, оставаясь в слоистой архитектуре     |
| 2026-09-22 | Скоуп развёрнут обратно: покупки и часть кабинета **входят** в текущую работу, не отложены                                                                          | Вчерашнее «MVP без покупок» было сказано поверхностно; бэк по факту готов сильно дальше                                                      |
| 2026-09-22 | Локальный бэк переключён на ветку `def`, снимок `context/backend/` обновлён с неё                                                                                   | Реальная работа велась там, не в `feat/frontend-integration`                                                                                 |
| 2026-09-22 | `TRIAL` и `EVENT` **не сливаются** в одну сущность БД – вместо этого новый эндпоинт-агрегатор на чтение                                                             | Слияние ослабляет уже рабочий constraint `Enrollment`, требует миграции протестированных данных; агрегатор решает задачу фронта без риска    |
| 2026-09-22 | Флоу регистрации: один экран email→код для входа и регистрации, анкета – обязательный шаг при пустом профиле                                                        | Уже так задокументировано на бэке (`auth-flow.md` §1.4) – теневая регистрация отклонена осознанно                                            |
| 2026-09-22 | Пагинация объединённого списка `TRIAL+EVENT` – на бэке, одним пагинатором поверх смёрженного списка                                                                 | Пагинация на фронте по двум независимым источникам не даёт верного порядка страниц                                                           |
| 2026-09-22 | Контентные заглушки (фото, отдельные цены/тексты) – не техдолг и не блокер, тестовые данные до финального прохода с хозяйкой                                        | Чтобы не тратить время на «починку» того, что и так заменится                                                                                |
| 2026-09-21 | Структура контекста – шесть файлов методологии Six-File плюс `api.md` и `content.md`                                                                                | Файлы разделены по стабильности: пять почти статичны, трекер меняется каждый шаг                                                             |
| 2026-09-21 | Доки бэка копируются снимком в `context/backend/`, а не дублируются пересказом                                                                                      | Пересказ разъезжается с источником. Снимок можно сравнить построчно                                                                          |
| 2026-09-21 | `context/backend/` добавлен в `.prettierignore`                                                                                                                     | Переформатирование ломает сравнение с источником                                                                                             |
| 2026-09-21 | Архитектура фронта – слоистая, не FSD                                                                                                                               | Доменов немного, команда один человек; `features`/`entities` дробить нечего                                                                  |
| 2026-09-14 | `devtools` и `typescript.typeCheck` выключены в `nuxt.config.ts`                                                                                                    | `vue-tsc` в dev-сервере съедает слишком много памяти. Проверка типов – отдельной командой                                                    |
| 2026-09-14 | `prerender` не используется, все публичные роуты на `ssr`                                                                                                           | Страницы фетчат бэк, а при сборке в Docker и CI его может не быть                                                                            |
| ранее      | Сервисы – классы с `ApiFetch` в конструкторе, фабрика `useXService()`                                                                                               | `useApi()` резолвится только в setup-контексте; класс не зависит от контекста Nuxt                                                           |
| ранее      | Маппинг DTO → доменная модель внутри сервиса, суффикс `VM` не используется                                                                                          | Компонент не должен видеть snake_case бэка                                                                                                   |
| ранее      | История фронта с трейлерами `Co-Authored-By` не переписывается                                                                                                      | Решение владельца. На новые коммиты не распространяется                                                                                      |

---

## Заметки для следующей сессии

- Начинать так: «Прочитай `CLAUDE.md` и `context/progress-tracker.md`, затем
  `<конкретная задача>`». Не «изучи проект».
- Одна задача – одна сессия. Не смешивать флоу регистрации, редизайн страниц
  покупки и правки кабинета в одной сессии – это разные куски плана.
- Перед началом работы: `cd ypa-center-backend && git status` – убедиться, что
  всё ещё на `def`; `docker compose up -d --build`, если контейнеры не
  пересобраны под новый код.
- Память ИИ – не источник правды о проекте. Источник – `context/` и код.
- При `ENVIRONMENT=local` письма уходят в консоль, тело в логе воркера
  закодировано base64. Код OTP берётся из БД:

    ```bash
    cd ypa-center-backend && .venv/bin/python manage.py shell -c "from apps.users.models import MagicTokens; t=MagicTokens.objects.filter(email='kirill-io3@yandex.ru', is_used=False).order_by('-created_at').first(); print(t.code, t.expires_at) if t else print('нет активного кода')"
    ```

- Тестовый родитель – `kirill-io3@yandex.ru` с одной дочерью. Демо-данные:
  `manage.py seed_demo` (только `DEBUG=True`). Cooldown OTP 60 с, TTL кода 5 мин.
- `docker compose ps` из другого cwd иногда возвращает пусто – надёжнее
  `docker ps --filter name=ypa-center-backend`.

# Конфиг локального pre-commit хука – переформатируется самим хуком
.github/hooks/

# Снимки документации бэкенда – должны совпадать байт в байт с источником,
# иначе их нельзя сравнить с ypa-center-backend/docs/
context/backend/
